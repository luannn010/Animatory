import { BackLink } from '@animatory/ui'

export const Default = () => (
  <BackLink onClick={() => {}}>Back to dashboard</BackLink>
)

export const ToTrack = () => (
  <BackLink onClick={() => {}}>Back to Character Design</BackLink>
)
