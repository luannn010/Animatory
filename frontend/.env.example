VITE_API_BASE_URL=http://localhost:8000
VITE_USE_MOCK=true

# Studio surface mock toggle (independent of VITE_USE_MOCK).
# true (default) = client-side mock, zero backend.
# false = hit the real /studio backend at VITE_API_BASE_URL.
VITE_STUDIO_USE_MOCK=true
